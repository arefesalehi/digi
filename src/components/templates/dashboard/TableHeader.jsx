

const TableHeader = ({children}) => {
  return (
    <div className="flex justify-between items-center border-gray-300 border-b-2 w-full h-12! overflow-hidden font-bold text-xs">{children}</div>
  )
}

export default TableHeader